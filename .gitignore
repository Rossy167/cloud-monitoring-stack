# Terraform
.terraform/
*.tfvars
!*.tfvars.example
*.tfstate
*.tfstate.*
crash.log
crash.*.log
tfplan
*.tfplan

# macOS
.DS_Store

# Claude Code local overrides (personal, not shared)
.claude/settings.local.json
